apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  labels:
    app: frontend
data:
  VITE_API_URL: "http://BACKEND_EXTERNAL_IP:5000"
  # Update BACKEND_EXTERNAL_IP after deploying backend service
  # kubectl edit configmap frontend-config -n todo-app
